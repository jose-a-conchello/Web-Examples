// an anomimous function. 
//  Because the function is enclosed in (), it becomes an expression
//  that is evaluated as soon as it is downladed.
//  This is called an "immediately invoqued function expression" (IIFE)
//  One advantage of the whole function being enclosed in () is that
//  neither the function nor its contents (variable and other funcitons)
//  are injected into the global namespace. The entire function is 
//  encapsulated
(function()  {
    "use stritct"; // prevents some errors

})();
